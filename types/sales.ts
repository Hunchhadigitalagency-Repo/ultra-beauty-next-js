export interface ISales {
    id: number;
    name: string;
    slug: string;
    images: string[];
    total_sold_quantity: number;
    total_earning: string;
    date: string
    order_id: string
}
